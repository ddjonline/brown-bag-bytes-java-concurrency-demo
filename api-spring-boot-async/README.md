# api-spring-boot-async

## Develop

Build the project

```shell
mvn -U clean verify
```

Run the project (Spring Boot dev mode)

```shell
mvn spring-boot:run
```

Run the project (Jvm Uber/Fat/Runner Jar)

```shell
mvn package
java -jar target/*.jar
```
